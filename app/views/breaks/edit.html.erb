<%= form_for @break do |f| %>
    <% if @break.errors.any? %>
<ul>
   <% @break.errors.full_messages.each do |m| %>
  <li><%= m %></li>
    <% end %>
</ul>
  <% end %>

  <%= f.label :review, "Review Your Stay!" %><br><br>
  <%= f.text_area :review %><br>
  <%= f.submit "Leave Review" %>
<% end %>
